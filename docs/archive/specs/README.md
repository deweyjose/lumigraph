# Archived Specs

These feature-spec folders are retained for historical context only.

- Do not treat them as active planning or execution state.
- The current source of truth for planning is `docs/ROADMAP.md` plus GitHub issues.
- If a historical spec conflicts with current code or docs, prefer the current code and docs.

Archived folders kept here are the ones that still help explain shipped work or design intent.
