# Root .env.example — used by Prisma CLI (pnpm db:migrate, pnpm db:studio, etc.)
# Next.js uses apps/web/.env instead — see apps/web/.env.example.

DATABASE_URL="postgresql://lumigraph:lumigraph@localhost:5432/lumigraph_db"
